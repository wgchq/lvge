<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->

        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">false</item>
        <item name="android:windowTranslucentStatus">false</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="my_buttonActiveStyle">
        <item name="android:gravity">center</item>
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:background">@drawable/buttonshape</item>
    </style>
    <!--登陆页面-->
    <style name="my_layoutStyle">
        <item name="android:padding">12dp</item>
    </style>

    <style name="ActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/ActionSheetDialogAnimation</item>
    </style>
    <!-- ActionSheet进出动画 -->
    <style name="ActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/actionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/actionsheet_dialog_out</item>
    </style>

    <style name="floatingSearchViewStyle">

    </style>

    <style name="popwindow_delete_btn_anim_style">
        <item name="android:windowEnterAnimation">@anim/delete_btn_show</item>
        <item name="android:windowExitAnimation">@anim/delete_btn_hide</item>
    </style>


    <style name="system_title_style">
        <item name="android:textColor">@color/mainFontColor</item>
        <item name="android:textSize">@dimen/x36</item>
    </style>

    <style name="system_hint_style">
        <item name="android:padding">@dimen/system_edit_view_padding</item>
        <item name="android:textSize">@dimen/system_hint_size</item>
    </style>

    <!--  tablayout字体大小-->
    <style name="customer_tablayout">
        <item name="android:textSize">@dimen/x28</item>
    </style>
</resources>
